# appspec.yml

version: 0.0
os: linux
files:
  - source: /
    destination: /home/ubuntu/docker/jdk/jar
    overwrite: yes

permissions:
  - object: /
    pattern: "**"
    owner: ubuntu
    group: ubuntu
